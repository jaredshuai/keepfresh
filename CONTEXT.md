# KeepFresh

个人物资保质期管理（HarmonyOS App）：只管入库与到期提醒，不追踪消耗/出入库流水。

> 范围变更（2026-08-24，pantry-logic 移植）：物资生命周期管理升级——引入状态机（在库/开封/用完/丢弃）与软删除；数量与位置管理增强。核心不变量保持：一条记录对应一件实物、端侧、无云端。

本文件只是领域词汇表。决策记录见 `docs/adr/`；新增字段接线见 `docs/agents/wiring-checklist.md`；视觉设计见 `DESIGN.md`。

## Language

**物资（Material）**:
一件具体的实物，一条记录对应一件实物；同名物品多次购入即多条记录，不做批次归组。
_Avoid_: 库存项、商品、批次

**入库**:
新增一条物资记录的动作。App 只管入库，不做出库/消耗/丢弃记录。
_Avoid_: 出入库、库存变动

**条形码**:
物资上的一维码。用于重复入库：扫历史中已出现过的条形码可快速再入库一件，避免重复填写表单。

**临期**:
距到期日剩余天数不大于临期阈值（默认 7 天，用户可配置）的状态。
_Avoid_: 快过期、预警

**已过期**:
到期日早于今天的状态。

**安全**:
既未临期也未过期的状态。

**到期日**:
生产日期 + 保质期（月数优先按月加法并 clamp 月末，否则按天数加）计算出的日期，是所有分级判断的基准。
包装只印「保质期至/到期日」的物品可走直录口径：直接录入到期日（production_date 为空即 direct 口径，
录入页双模式切换）。两种口径的分级/提醒/统计完全同权。

**同批次合并**:
同名 + 同位置 + 同到期日 + 同单位 的在库记录视为同一批次：新增入库自动并入既有记录（数量求和）
而非另起一条；启动时对「无损重复」（分类/备注/自定义字段完全一致，重复行不携带独有信息）做一次并条。
携带独有信息的重复不并——宁可两条，不可静默丢数。开封/终态记录不参与并条。
注意：重复条目可能是自然工作流产物（同批分放不同位置、录入时不知全量、翻到再扫码快录），
**跨位置的重复是预期状态，不是脏数据**（ADR-0003）。

**自定义字段**:
用户可为物资自行添加的扩展字段（名称 + 类型），随物资记录保存。用于应对随时新增的属性需求。
_Avoid_: 扩展属性、标签（与分类混淆）

**后台代理提醒**:
由系统（而非 App 进程）在设定时间发出的提醒；App 未打开也能触达。区别于仅在 App 启动时发布的应用内通知。

**扫码快填**:
AddItem 表单页的扫码入口：ScanKit 扫码后按条形码匹配历史，点选建议即预填表单。

**搜索建议**:
扫码后按条形码匹配出的历史入库记录列表（名称 + 最近入库时间），点选其一预填表单；不自动填充。

**拍照入库**:
首页独立入口：拍照后由端侧识别生成表单预填，用户确认后才入库。识别结果一律进表单待确认，绝不自动入库。
（注：该功能已放弃，见 ADR-0002；词条保留以解释历史代码与文档中的引用。）

**端侧**:
识别能力全部在本机执行的属性。KeepFresh 的所有 AI/识别功能必须端侧，禁止任何云端调用。
