# 调研任务：鸿蒙数据备份与导出规范（ticket #2）

## 你的角色

你是一位 HarmonyOS（鸿蒙）应用开发研究员。请**独立**完成下列调研——不要参考本文件"现有初步结论"之外的信息，全程独立查证。你产出的报告会与另外两个 AI 的独立报告交叉对比，因此请只写你亲自验证过的事实，不写"据说/应该"。

## 项目背景（1 分钟）

- 项目：KeepFresh，鸿蒙原生个人 App，物资保质期管理（RelationalStore/SQLite 存物资，无账号、纯本地）。
- 工程基线：API 24（compatibleSdkVersion 6.1.1(24)），@kit.ArkData 的 relationalStore。
- 需求：做"备份/导出 + 恢复/导入"功能，让用户在换手机/误删时能找回数据。

## 调研问题（逐条回答）

1. RelationalStore（@kit.ArkData / relationalStore）是否有官方推荐的"导出数据库文件"或"备份/恢复" API？如果有，给出 API 名与限制；如果没有，官方建议的替代做法是什么？
2. 在 HarmonyOS 中，把文件保存到"用户可访问的公共位置"（如文档、下载目录）有哪些途径？各自是否**免权限**？对比：应用沙箱目录、直接写公共目录、系统文件选择器（DocumentViewPicker.save() 等）。
3. DocumentViewPicker.save() 的完整签名与行为：参数（newFileNames / fileSuffixChoices / defaultFilePathUri 等）、返回值、是否免权限、典型用法（配合 fs 写入的完整代码链路）。
4. 从用户选择的文件导入回读：DocumentViewPicker.select() 的签名、fileSuffixFilters 用法、配合 fs 读取的完整链路。
5. 官方是否存在"应用数据备份恢复框架"？它面向什么场景，是否适用于"应用内导出 JSON 给用户自存"？

## 权威来源指引（按优先级）

1. 华为官方文档站 developer.huawei.com（注意：正文是 JS 渲染的 SPA，直接抓取会拿不到内容；可用其 alternate markdown 链接、站内搜索，或通过可靠的第三方转载核对 API 签名）
2. ctx7 / context7（HarmonyOS 文档索引）——能检索官方 API 参考则优先用
3. 华为开发者社区 / 高质量实践文章（CSDN、掘金、51CTO，发布日期 2024 年之后）
4. 找不到确切答案的：**如实写"未查证"**，禁止编造 API 名、参数或错误码

## 输出格式（严格遵守，直接输出以下 Markdown 模板）

```markdown
## 一句话结论
<直接回答调研问题的结论>

## 事实明细
| # | 对应问题 | 事实 | 来源(链接或书名) | 可靠性(官方/社区/未验证) |

## 对"现有初步结论"的逐条核验
对下方"现有初步结论"中每一条标注：✅同意 / ❌反驳 / ➕补充，并给一句话理由。
（没有要核验的条目就写"无"）

## 未查证事项
<你尝试查但没有确切答案的问题列表>

## 对 KeepFresh 的落地建议
<基于你的结论，给 2-4 条可执行的建议>

## 来源列表
<所有你实际引用过的链接，逐条列出>
```

## 现有初步结论（待你独立核验）

- 鸿蒙无"导出数据库文件"的官方 API，应用内导出应"读出记录 → JSON 序列化"。
- 推荐用 DocumentViewPicker（save 导出 / select 导入），全程免文件权限（由用户在系统 UI 授权）。
- 应用沙箱目录用户取不出，直接写公共目录需要权限，两者都不适合。

## 铁律

- 每个事实必须有来源；没有来源的结论不算数。
- 未查证就写"未查证"，编造 = 失败。
- 输出必须完全遵循上面的格式模板，不要添加模板外的大段说明。
